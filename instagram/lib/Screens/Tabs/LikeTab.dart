import 'package:flutter/material.dart';

class LikeTab extends StatelessWidget {
  const LikeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Like Tab')),
    );
  }
}
